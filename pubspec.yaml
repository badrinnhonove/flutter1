name: Boda al alamy
description: A new Flutter project by Badr Abou zed Ghazawy.

publish_to: 'none' # إذا لم تكن تنوي رفع الحزمة إلى pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
